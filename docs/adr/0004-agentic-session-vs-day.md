# Agentic Session is idle-timeout, not the Check-in Day

A **Session** is one in-flight conversation per chat (serialized turns, idle close → memory refactor). A **Day** is the calendar ledger until Deadline. Rejected: no Session (one-shot turns only — weak multi-message talk) and Session = whole open Day (zombie context, token burn). Many Sessions may open/close during one Day.
