# Deadline may cross midnight; Day keyed to evening Reminder

Evening groups often want Reminder ~21:00 and Deadline after midnight. The **Day** identity stays the Reminder cycle’s evening date, not the Deadline’s clock date. Rejected: same-calendar-date-only Deadline (forces early close) and “Deadline = always midnight” (less control in `/settings`).
