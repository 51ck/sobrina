{
  "name": "sobrina",
  "private": true,
  "type": "module",
  "workspaces": ["packages/*"],
  "scripts": {
    "lint": "echo \"lint: wire in T3\" && exit 0",
    "typecheck": "echo \"typecheck: wire in T2\" && exit 0"
  }
}
