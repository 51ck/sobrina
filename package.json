{
  "name": "sobrina",
  "private": true,
  "type": "module",
  "workspaces": ["packages/*"],
  "scripts": {
    "lint": "echo \"lint: wire in T3\" && exit 0",
    "typecheck": "bun run --filter '@sobrina/*' typecheck"
  },
  "devDependencies": {
    "typescript": "^7.0.2"
  }
}
